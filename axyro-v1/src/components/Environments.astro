<section class="environments reveal" id="environments">
  <div class="heading">
    <div class="left">
      <p class="eyebrow">ENVIRONMENTS</p>

      <h2>
        Built for different<br />
        kinds of focus.
      </h2>
    </div>

    <div class="right">
      <p>
        Calm operational states shaped for clarity, depth and control.
        Each environment is a fragment of the AXYRO system.
      </p>
    </div>
  </div>

  <div class="cards">
    <a
      class="card reveal reveal-delay-1"
      href="/environments/blue-horizon"
    >
      <div class="axyro-image">
        <img
          src="/images/env-horizon.png"
          alt="Blue Horizon"
        />
      </div>

      <div class="content">
        <span class="type">NORMAL</span>

        <h3>Blue Horizon</h3>

        <p>
          Clarity through space, silence and atmosphere.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-2"
      href="/environments/soft-grid"
    >
      <div class="axyro-image">
        <img
          src="/images/env-grid.png"
          alt="Soft Grid"
        />
      </div>

      <div class="content">
        <span class="type">NORMAL</span>

        <h3>Soft Grid</h3>

        <p>
          Structural calm for focused workflows.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-3"
      href="/environments/black-core"
    >
      <div class="axyro-image">
        <img
          src="/images/env-core.png"
          alt="Black Core"
        />
      </div>

      <div class="content">
        <span class="type">POWER</span>

        <h3>Black Core</h3>

        <p>
          Controlled darkness. Silent capability.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-4"
      href="/environments/data-field"
    >
      <div class="axyro-image">
        <img
          src="/images/env-depth.png"
          alt="Data Field"
        />
      </div>

      <div class="content">
        <span class="type">POWER</span>

        <h3>Data Field</h3>

        <p>
          Invisible systems beneath the surface.
        </p>
      </div>
    </a>
  </div>
</section>

<style>
  .environments {
    position: relative;

    padding:
      112px
      clamp(32px, 7vw, 120px)
      132px;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 50% 0%,
        rgba(167,216,255,0.035),
        transparent 30%
      ),
      linear-gradient(
        180deg,
        #030712 0%,
        #020407 100%
      );

    border-top:
      1px solid rgba(255,255,255,0.045);
  }

  .environments::before {
    content: "";

    position: absolute;
    left: 0;
    right: 0;
    top: 42%;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.08),
        transparent
      );

    opacity: 0.34;
  }

  .heading {
    position: relative;
    z-index: 1;

    display: grid;

    grid-template-columns:
      1fr 520px;

    gap:
      clamp(56px, 8vw, 120px);

    align-items: end;
  }

  .eyebrow {
    margin-bottom: 26px;
  }

  h2 {
     max-width: 760px;

  font-size: clamp(50px, 5vw, 82px);

  line-height: 0.9;
  letter-spacing: -0.085em;
  }

  .right p {
    max-width: 520px;

    font-size: 18px;
    line-height: 1.8;
  }

  .cards {
    position: relative;
    z-index: 1;

    margin-top: 88px;

    display: grid;

    grid-template-columns:
      1.25fr
      1fr
      0.92fr
      0.82fr;

    gap: 26px;

    align-items: start;
  }

  .card {
    position: relative;

    display: block;

    overflow: hidden;

    border-radius: 30px;

    text-decoration: none;
    color: inherit;

    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.018),
        rgba(255,255,255,0.006)
      );

    border:
      1px solid rgba(255,255,255,0.035);

    transition:
      transform 0.8s cubic-bezier(.19, 1, .22, 1),
      border-color 0.7s ease,
      box-shadow 0.7s ease;
  }

  .card:nth-child(1) {
    aspect-ratio: 0.74;
  }

  .card:nth-child(2) {
    aspect-ratio: 0.92;

    margin-top: 52px;
  }

  .card:nth-child(3) {
    aspect-ratio: 0.68;
  }

  .card:nth-child(4) {
    aspect-ratio: 0.82;

    margin-top: 92px;
  }

  .card::before {
    content: "";

    position: absolute;
    inset: 0;

    z-index: 2;

    background:
      linear-gradient(
        180deg,
        rgba(2,4,7,0.08) 0%,
        rgba(2,4,7,0.22) 42%,
        rgba(2,4,7,0.82) 100%
      );

    pointer-events: none;
  }

  .card::after {
    content: "";

    position: absolute;

    left: 12%;
    right: 12%;
    bottom: 30%;

    z-index: 3;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.22),
        transparent
      );

    opacity: 0.18;

    pointer-events: none;
  }

  .card:hover {
    transform:
      translateY(-8px);

    border-color:
      rgba(167,216,255,0.10);

    box-shadow:
      0 24px 80px rgba(0,0,0,0.26),
      0 0 42px rgba(167,216,255,0.026);
  }

  .axyro-image {
    position: absolute;
    inset: 0;

    overflow: hidden;
  }

  .axyro-image img {
    position: absolute;
    inset: 0;

    width: 100%;
    height: 100%;

    object-fit: cover;
    object-position: center;

    filter:
      brightness(0.92)
      contrast(1.08)
      saturate(0.86);

    transform: scale(1.02);

    transition:
      transform 1.8s cubic-bezier(.19, 1, .22, 1),
      filter 1.8s ease;
  }

  .card:nth-child(3) img {
    filter:
      brightness(0.82)
      contrast(1.14)
      saturate(0.72);
  }

  .card:hover img {
    transform:
      scale(1.05);

    filter:
      brightness(1)
      contrast(1.12)
      saturate(0.92);
  }

  .content {
    position: absolute;

    left: 30px;
    right: 30px;
    bottom: 40px;

    z-index: 4;
  }

  .type {
    display: block;

    font-size: 10px;
    letter-spacing: 0.38em;
    text-transform: uppercase;

    color:
      rgba(167,216,255,0.76);
  }

  h3 {
    margin-top: 16px;
    margin-bottom: 12px;

    font-size:
      clamp(26px, 2vw, 34px);

    line-height: 0.96;
  }

  .content p {
    max-width: 260px;

    font-size: 15px;
    line-height: 1.7;

    color:
      rgba(245,247,250,0.56);
  }

  @media (max-width: 1200px) {
    .heading {
      grid-template-columns: 1fr;
    }

    .cards {
      grid-template-columns: 1fr 1fr;
    }

    .card:nth-child(2),
    .card:nth-child(4) {
      margin-top: 0;
    }
  }

  @media (max-width: 720px) {
    .environments {
      padding:
        84px
        24px
        96px;
    }

    .heading {
      grid-template-columns: 1fr;

      gap: 36px;
    }

    .cards {
      grid-template-columns: 1fr;

      gap: 22px;
    }

    .card,
    .card:nth-child(1),
    .card:nth-child(2),
    .card:nth-child(3),
    .card:nth-child(4) {
      aspect-ratio: 1.02;

      margin-top: 0;
    }

    h2 {
      font-size:
        clamp(48px, 13vw, 68px);
    }

    .right p {
      font-size: 17px;
    }
  }
</style>