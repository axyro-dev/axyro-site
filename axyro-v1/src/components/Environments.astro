<section class="environments reveal" id="environments">
  <div class="heading">
    <div class="left">
      <p class="eyebrow">ENVIRONMENTS</p>

      <h2>
        Built for different<br />
        kinds of focus.
      </h2>
    </div>

    <div class="right">
      <p>
        Calm operational states shaped for clarity, depth and control.
        Each environment is a fragment of the AXYRO system.
      </p>
    </div>
  </div>

  <div class="cards">
    <a
      class="card reveal reveal-delay-1"
      href="/environments/blue-horizon"
    >
      <div class="axyro-image">
        <img
          src="/wallpapers/axyro_blue_horizon_v2_4kd.png"
          alt="Blue Horizon"
        />
      </div>

      <div class="content">
        <span class="type">NORMAL</span>

        <h3>Blue Horizon</h3>

        <p>
          Clarity through space, silence and atmosphere.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-2"
      href="/environments/soft-grid"
    >
      <div class="axyro-image">
        <img
          src="/wallpapers/axyro_soft_grid_v1_4kd.png"
          alt="Soft Grid"
        />
      </div>

      <div class="content">
        <span class="type">NORMAL</span>

        <h3>Soft Grid</h3>

        <p>
          Structural calm for focused workflows.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-3"
      href="/environments/black-core"
    >
      <div class="axyro-image">
        <img
          src="/wallpapers/axyro_power_black_core_v1_4kd.png"
          alt="Black Core"
        />
      </div>

      <div class="content">
        <span class="type">POWER</span>

        <h3>Black Core</h3>

        <p>
          Controlled darkness. Silent capability.
        </p>
      </div>
    </a>

    <a
      class="card reveal reveal-delay-4"
      href="/environments/data-field"
    >
      <div class="axyro-image">
        <img
          src="/wallpapers/axyro_power_data_field_v2_4kd.png"
          alt="Data Field"
        />
      </div>

      <div class="content">
        <span class="type">POWER</span>

        <h3>Data Field</h3>

        <p>
          Invisible systems beneath the surface.
        </p>
      </div>
    </a>
  </div>
</section>

<style>
  .environments {
    position: relative;

    padding:
      112px
      clamp(32px, 7vw, 120px)
      132px;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 50% 0%,
        rgba(167,216,255,0.035),
        transparent 30%
      ),
      linear-gradient(
        180deg,
        #030712 0%,
        #020407 100%
      );

    border-top:
      1px solid rgba(255,255,255,0.045);
  }

  .environments::before {
  content: "";

  position: absolute;
  inset: 0;

  background:
    radial-gradient(
      circle at 50% 20%,
      rgba(142,223,255,0.028),
      transparent 42%
    ),
    linear-gradient(
      180deg,
      transparent,
      rgba(255,255,255,0.008),
      transparent
    );

  opacity: 0.7;

  pointer-events: none;
}

  .heading {
    position: relative;
    z-index: 1;

    display: grid;

    grid-template-columns:
      1fr 520px;

    gap:
      clamp(56px, 8vw, 120px);

    align-items: end;
  }

  .eyebrow {
    margin-bottom: 26px;
  }

  h2 {
    max-width: 760px;

    font-size: clamp(50px, 5vw, 82px);

    line-height: 0.9;
    letter-spacing: -0.085em;
  }

  .right p {
    max-width: 520px;

    font-size: 18px;
    line-height: 1.8;
  }

  .cards {
    position: relative;
    z-index: 1;

    margin-top: 88px;

    display: grid;

    grid-template-columns:
      1.25fr
      1fr
      0.92fr
      0.82fr;

    gap: 26px;

    align-items: start;
  }

  .card {
    position: relative;
    isolation: isolate;
    display: block;

    overflow: hidden;

    border-radius: 30px;

    text-decoration: none;
    color: inherit;

    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.018),
        rgba(255,255,255,0.006)
      );

    border:
      1px solid rgba(255,255,255,0.014);

    transition:
      transform 0.8s cubic-bezier(.19, 1, .22, 1),
      border-color 0.7s ease,
      box-shadow 0.7s ease;
      
  }

  .card:nth-child(1) {
    aspect-ratio: 0.74;
  }

  .card:nth-child(2) {
    aspect-ratio: 0.92;

    margin-top: 52px;
  }

  .card:nth-child(3) {
    aspect-ratio: 0.68;
  }

  .card:nth-child(4) {
    aspect-ratio: 0.82;

    margin-top: 92px;
  }


 .cards::before {
  content: "";

  position: absolute;
  inset: -10%;

  background:
    radial-gradient(
      circle at center,
      rgba(142,223,255,0.03),
      transparent 60%
    );

  pointer-events: none;
}

.card::before {
  content: "";

  position: absolute;
  inset: 0;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,0.03),
      transparent 28%
    );

  opacity: 0.24;

  pointer-events: none;
  z-index: 2;
}
  .card::after {
    content: "";

    position: absolute;

    left: 12%;
    right: 12%;
    bottom: 30%;

    z-index: 3;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.22),
        transparent
      );

    opacity: 0.18;

    pointer-events: none;
  }

 .card:hover {
   transform:
    translateY(-2px);

  border-color:
    rgba(167,216,255,0.04);

  box-shadow:
    0 12px 38px rgba(0,0,0,0.18),
    0 0 18px rgba(167,216,255,0.01);
}

  .axyro-image {
    position: absolute;
    inset: 0;

    overflow: hidden;
  }

  .axyro-image img {
    position: absolute;
    inset: 0;

    width: 100%;
    height: 100%;

    object-fit: cover;
    object-position: center;

    filter:
       brightness(0.9)
       contrast(1.04)
       saturate(0.86);

   transform: scale(1.025);

    transition:
      transform 1.8s cubic-bezier(.19, 1, .22, 1),
      filter 1.8s ease;
  }

  .card:nth-child(3) img,
  .card:nth-child(4) img {
    filter:
      brightness(0.82)
      contrast(1.05)
      saturate(0.74);
  }

  .card:hover img {
    transform:
      scale(1.035);

    filter:
      brightness(0.9)
      contrast(1.08)
      saturate(0.88);
  }

  .content {
    position: absolute;

    left: 30px;
    right: 30px;
    bottom: 34px;

    z-index: 4;
  }

  .type {
    display: block;

    font-size: 10px;
    letter-spacing: 0.38em;
    text-transform: uppercase;

    color:
      rgba(167,216,255,0.76);
  }

  h3 {
    margin-top: 18px;
    margin-bottom: 14px;

    font-size:
      clamp(26px, 2vw, 34px);

    line-height: 0.96;
  }

  .content p {
    max-width: 260px;

    font-size: 15px;
    line-height: 1.7;

    color:
      rgba(245,247,250,0.56);
  }

  @media (max-width: 1200px) {
    .heading {
      grid-template-columns: 1fr;
    }

    .cards {
      grid-template-columns: 1fr 1fr;
    }

    .card:nth-child(2),
    .card:nth-child(4) {
      margin-top: 0;
    }
  }

  @media (max-width: 720px) {
    .environments {
      padding:
        84px
        24px
        96px;
    }

    .heading {
      grid-template-columns: 1fr;

      gap: 36px;
    }

    .cards {
      grid-template-columns: 1fr;

      gap: 22px;
    }

    .card,
    .card:nth-child(1),
    .card:nth-child(2),
    .card:nth-child(3),
    .card:nth-child(4) {
      aspect-ratio: 1.02;

      margin-top: 0;
    }

    h2 {
  font-size: clamp(44px, 12vw, 62px);
  line-height: 0.92;
  letter-spacing: -0.07em;
  font-weight: 420;
   }

    .right p {
      font-size: 17px;
    }
  }
</style>