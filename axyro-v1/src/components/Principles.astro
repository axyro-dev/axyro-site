<section class="principles" id="philosophy">
  <div class="top reveal">
    <div class="left">
      <p class="eyebrow">OUR PRINCIPLES</p>

      <p class="statement">
        Open underneath,<br />
        curated on top.
      </p>
    </div>

    <div class="right">
      <h2>
        Freedom with standards.
      </h2>

      <p>
        AXYRO combines open foundations with a restrained, carefully curated
        surface designed for calm and focused compute.
      </p>
    </div>
  </div>

  <div class="grid">
    <article class="card reveal reveal-delay-1">
      <span class="index">01</span>
      <div class="orb"></div>
      <h3>Focus</h3>
      <p>Remove noise. Keep only what supports the work.</p>
    </article>

    <article class="card reveal reveal-delay-2">
      <span class="index">02</span>
      <div class="orb"></div>
      <h3>Speed</h3>
      <p>Lightweight by default. Capable when required.</p>
    </article>

    <article class="card reveal reveal-delay-3">
      <span class="index">03</span>
      <div class="orb"></div>
      <h3>Control</h3>
      <p>Open underneath. Curated at the surface.</p>
    </article>

   <article class="card reveal reveal-delay-3">
      <span class="index">04</span>
      <div class="orb"></div>
      <h3>Precision</h3>
      <p>Every element has a reason to exist.</p>
    </article>
  </div>
</section>

<style>
  .principles {
    position: relative;
    padding: 104px clamp(42px, 7vw, 132px) 118px;
    overflow: hidden;

    background:
      radial-gradient(
        ellipse at 72% 10%,
        rgba(167,216,255,0.025),
        transparent 38%
      ),
      linear-gradient(
        180deg,
        #020407 0%,
        #05070A 100%
      );

    border-top: 1px solid rgba(255,255,255,0.035);
  }

  .principles::before {
    content: "";
    position: absolute;
    left: 50%;
    top: 0;
    width: 1px;
    height: 100%;

    background:
      linear-gradient(
        180deg,
        rgba(167,216,255,0),
        rgba(167,216,255,0.045),
        rgba(167,216,255,0)
      );

    opacity: 0.24;
  }

  .top {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 320px 1fr;
    gap: clamp(64px, 9vw, 154px);
    align-items: start;
  }

  .eyebrow {
    margin: 0;
    font-size: 11px;
    letter-spacing: 0.42em;
    text-transform: uppercase;
    color: rgba(167,216,255,0.66);
  }

  .statement {
    margin: 34px 0 0;
    max-width: 320px;
    font-size: clamp(34px, 3vw, 46px);
    line-height: 1.02;
    letter-spacing: -0.06em;
    font-weight: 430;
    color: rgba(245,247,250,0.28);
  }

  h2 {
    margin: 0;
    max-width: 820px;
    font-size: clamp(48px, 5vw, 78px);
    line-height: 0.94;
    letter-spacing: -0.07em;
    font-weight: 440;
    color: rgba(245,247,250,0.94);
  }

  .right p {
    margin: 30px 0 0;
    max-width: 620px;
    font-size: 18px;
    line-height: 1.72;
    color: rgba(245,247,250,0.48);
  }

  .grid {
    position: relative;
    z-index: 1;
    margin-top: 82px;

    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 28px;
  }

  .card {
    position: relative;
    min-height: 224px;
    padding: 34px 32px 32px;

    border: 1px solid rgba(255,255,255,0.022);

    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.006),
        rgba(255,255,255,0.001)
      );

    transition:
      transform 0.6s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.6s ease,
      background 0.6s ease;
  }

  .card:hover {
    transform: translateY(-3px);
    border-color: rgba(167,216,255,0.055);
    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.012),
        rgba(255,255,255,0.002)
      );
  }

  .index {
    position: absolute;
    right: 28px;
    top: 28px;
    font-size: 10px;
    letter-spacing: 0.32em;
    color: rgba(245,247,250,0.18);
  }

  .orb {
    width: 20px;
    height: 20px;
    border-radius: 999px;

    background:
      radial-gradient(
        circle,
        rgba(167,216,255,0.11),
        rgba(167,216,255,0.012) 70%
      );

    border: 1px solid rgba(167,216,255,0.055);
    box-shadow: 0 0 24px rgba(167,216,255,0.025);
  }

  h3 {
    margin: 48px 0 0;
    font-size: 22px;
    line-height: 1;
    letter-spacing: -0.04em;
    font-weight: 520;
    color: rgba(245,247,250,0.9);
  }

  .card p {
    margin: 16px 0 0;
    max-width: 250px;
    font-size: 15px;
    line-height: 1.65;
    color: rgba(245,247,250,0.42);
  }

  @media (max-width: 1100px) {
    .top {
      grid-template-columns: 1fr;
      gap: 54px;
    }

    .grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 720px) {
    .principles {
      padding: 78px 24px 88px;
    }

    .grid {
      grid-template-columns: 1fr;
    }

    h2 {
      font-size: clamp(44px, 12vw, 60px);
    }
  }
</style>