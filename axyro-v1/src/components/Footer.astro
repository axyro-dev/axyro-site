<footer class="footer">
  <div class="footer-inner">
    <p class="footer-statement">
      Reduce noise.
    </p>

    <div class="footer-bottom">
      <span class="footer-brand">
        AXYRO
      </span>

      <div class="footer-links">
        <a href="https://x.com/axyro_io" target="_blank" rel="noreferrer">X</a>
        <a href="https://www.instagram.com/axyro.io/" target="_blank" rel="noreferrer">Instagram</a>
        <a href="https://www.tiktok.com/@axyro.io" target="_blank" rel="noreferrer">TikTok</a>
      </div>

      <span class="footer-phase">
        ENVIRONMENTS / PHASE 1
      </span>
    </div>
  </div>
</footer>

<style>
  .footer {
    position: relative;

    padding:
      128px
      clamp(28px, 6vw, 120px)
      42px;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 50% 0%,
      rgba(167,216,255,0.025)
        transparent 34%
      ),
      linear-gradient(
        180deg,
        rgba(3,7,11,0.96) 0%,
        #05070A 100%
      );

    border-top:
      1px solid rgba(255,255,255,0.045);
  }

  .footer::before {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    top: 0;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.14),
        transparent
      );

    opacity: 0.45;
  }

  .footer::after {
    content: "";
    position: absolute;
    left: 50%;
    bottom: 118px;

    width: 62vw;
    max-width: 980px;
    height: 1px;

    transform: translateX(-50%);

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.14),
        transparent
      );

    opacity: 0.18;
  }

  .footer-inner {
    position: relative;
    z-index: 1;

    max-width: 1600px;
    margin: 0 auto;
  }

  .footer-statement {
    margin: 0;

    max-width: 900px;

    font-size: clamp(40px, 6vw, 96px);
    line-height: 0.9;
    letter-spacing: -0.065em;
    font-weight: 430;

    color: rgba(245,247,250,0.96);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;

    gap: 32px;

    margin-top: 96px;
  }

  .footer-brand,
  .footer-phase,
  .footer-links a {
    font-size: 10px;
    text-transform: uppercase;
  }

  .footer-brand {
    letter-spacing: 0.44em;

    color: rgba(245,247,250,0.32);
  }

  .footer-phase {
    letter-spacing: 0.46em;

    color: rgba(167,216,255,0.38);
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 22px;
  }

  .footer-links a {
    letter-spacing: 0.32em;
    text-decoration: none;

    color: rgba(245,247,250,0.34);

    transition: color 0.35s ease;
  }

  .footer-links a:hover {
    color: rgba(167,216,255,0.72);
  }

  @media (max-width: 720px) {
    .footer {
      padding:
        88px
        28px
        42px;
    }

    .footer-statement {
      font-size: clamp(36px, 11vw, 52px);
      line-height: 0.94;
    }

    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;

      gap: 26px;

      margin-top: 68px;
    }

    .footer-links {
      flex-wrap: wrap;
      gap: 18px;
    }

    .footer-phase {
      line-height: 1.8;
    }

    .footer::after {
      bottom: 168px;
      opacity: 0.12;
    }
  }
</style>