<footer class="footer">
  <div class="footer-inner">
    <p class="footer-statement">
      Reduce noise.
    </p>

  <div class="footer-bottom">
  <span class="footer-brand">
    AXYRO
  </span>

  <div class="footer-links">
    <a href="https://x.com/axyro_io" target="_blank">X</a>
    <a href="https://www.instagram.com/axyro.io/" target="_blank">Instagram</a>
    <a href="https://www.tiktok.com/@axyro.io" target="_blank">TikTok</a>
  </div>

  <span class="footer-phase">
    ENVIRONMENTS / PHASE 1
  </span>
</div>
</footer>

<style>
  .footer {
    position: relative;

    padding:
      128px
      clamp(28px, 6vw, 120px)
      42px;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 50% 0%,
        rgba(167,216,255,0.055),
        transparent 34%
      ),
      linear-gradient(
        180deg,
        rgba(3, 7, 11, 0.96) 0%,
        #05070A 100%
      );

    border-top:
      1px solid rgba(255,255,255,0.055);
  }

  .footer::before {
    content: "";

    position: absolute;
    left: 0;
    right: 0;
    top: 0;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.16),
        transparent
      );

    opacity: 0.55;
  }

  .footer::after {
    content: "";

    position: absolute;
    left: 50%;
    bottom: 118px;

    width: 62vw;
    max-width: 980px;
    height: 1px;

    transform: translateX(-50%);

    background:
      linear-gradient(
        90deg,
        transparent,
        rgba(167,216,255,0.18),
        transparent
      );

    opacity: 0.22;
  }

  .footer-inner {
    position: relative;
    z-index: 1;

    max-width: 1600px;
    margin: 0 auto;
  }

  .footer-statement {
    margin: 0;

    max-width: 900px;

    font-size: clamp(44px, 6vw, 96px);

    letter-spacing: -0.065em;

    line-height: 0.9;

    font-weight: 650;

    color: rgba(245,247,250,0.96);
  }

  .footer-bottom {
    display: flex;

    justify-content: space-between;

    align-items: flex-end;

    gap: 32px;

    margin-top: 96px;
  }

  .footer-brand {
    font-size: 11px;

    letter-spacing: 0.44em;

    text-transform: uppercase;

    color: rgba(245,247,250,0.32);
  }

  .footer-phase {
    font-size: 10px;

    letter-spacing: 0.46em;

    text-transform: uppercase;

    color: rgba(167,216,255,0.38);
  }

  @media (max-width: 720px) {
    .footer {
      padding:
        104px
        clamp(28px, 6vw, 120px)
        42px;
    }

    .footer-bottom {
      align-items: flex-start;

      flex-direction: column;

      margin-top: 72px;
    }

    .footer-phase {
      line-height: 1.8;
    }
  }
  .footer-links {
  display: flex;
  gap: 22px;
  align-items: center;
}

.footer-links a {
  font-size: 10px;
  letter-spacing: 0.32em;
  text-transform: uppercase;
  text-decoration: none;
  color: rgba(245,247,250,0.34);
}

.footer-links a:hover {
  color: rgba(167,216,255,0.72);
}
.footer-links {
  display: flex;
  gap: 22px;
  align-items: center;
}

.footer-links a {
  font-size: 10px;
  letter-spacing: 0.32em;
  text-transform: uppercase;
  text-decoration: none;
  color: rgba(245,247,250,0.34);
}

.footer-links a:hover {
  color: rgba(167,216,255,0.72);
}
</style>