<header class="nav">
 <div class="brand-lockup">
  <span class="brand-symbol">AX</span>

  <span class="brand-divider"></span>

  <span class="brand-wordmark">
    A X Y R O
  </span>
</div>

  <nav class="links">
    <a href="/">Overview</a>
    <a href="/environments">Environments</a>
    <a href="/philosophy">Philosophy</a>
    <a href="/design">Design</a>
    <a href="/journal">Journal</a>
  </nav>

  <a class="cta" href="/support">
    Support AXYRO
  </a>
</header>

<style>
  .nav {
    position: fixed;

    top: 18px;
    left: 24px;
    right: 24px;

    height: 64px;

    z-index: 1000;

    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 0 26px;

    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.035),
        rgba(255,255,255,0.012)
      );

    backdrop-filter: blur(18px);

    border:
      1px solid rgba(255,255,255,0.035);

    border-radius: 18px;

    box-shadow:
      0 10px 40px rgba(0,0,0,0.28);

    transition:
      background 0.45s ease,
      border-color 0.45s ease,
      box-shadow 0.45s ease,
      transform 0.45s ease,
      top 0.45s ease;
  }

  .nav.scrolled {
    top: 14px;

    background:
      linear-gradient(
        180deg,
        rgba(255,255,255,0.05),
        rgba(255,255,255,0.018)
      );

    border-color:
      rgba(167,216,255,0.08);

    box-shadow:
      0 18px 60px rgba(0,0,0,0.34);
  }

  .brand {
    display: flex;
    align-items: center;

    gap: 12px;

    width: fit-content;

    text-decoration: none;
  }

  .brand *,
  .brand:hover,
  .brand:visited {
    text-decoration: none;
  }

  .mark-logo {
    width: 22px;
    height: 22px;

    object-fit: contain;

    filter:
      drop-shadow(
        0 0 12px rgba(167,216,255,0.18)
      );

    opacity: 0.96;

    transition:
      transform 0.45s ease,
      filter 0.45s ease;
  }

  .nav:hover .mark-logo {
    transform: rotate(-4deg) scale(1.04);

    filter:
      drop-shadow(
        0 0 18px rgba(167,216,255,0.24)
      );
  }

  .wordmark {
    font-size: 12px;

    letter-spacing: 0.38em;

    font-weight: 500;

    color:
      rgba(245,247,250,0.88);
  }

  .links {
    display: flex;

    gap: 46px;
  }

  .links a {
    position: relative;

    font-size: 13px;

    color:
      rgba(245,247,250,0.78);

    transition:
      color 0.35s ease,
      opacity 0.35s ease;
  }

  .links a::after {
    content: "";

    position: absolute;

    left: 0;
    bottom: -10px;

    width: 100%;
    height: 1px;

    background:
      linear-gradient(
        90deg,
        rgba(167,216,255,0.0),
        rgba(167,216,255,0.72),
        rgba(167,216,255,0.0)
      );

    opacity: 0;

    transform: scaleX(0.6);

    transition:
      opacity 0.35s ease,
      transform 0.35s ease;
  }

  .links a:hover {
    color:
      rgba(245,247,250,0.96);
  }

  .links a:hover::after {
    opacity: 1;
    transform: scaleX(1);
  }

  .cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    height: 42px;

    padding: 0 18px;

    border-radius: 999px;

    background:
      rgba(255,255,255,0.035);

    border:
      1px solid rgba(255,255,255,0.08);

    color:
      rgba(245,247,250,0.88);

    font-size: 13px;

    letter-spacing: 0.02em;

    transition:
      background 0.35s ease,
      border-color 0.35s ease,
      transform 0.35s ease,
      box-shadow 0.35s ease;
  }

  .cta:hover {
    transform: translateY(-1px);

    background:
      rgba(255,255,255,0.09);

    border-color:
      rgba(167,216,255,0.14);

    box-shadow:
      0 0 24px rgba(167,216,255,0.06);
  }

  @media (max-width: 980px) {
    .links {
      display: none;
    }
  }
@media (max-width: 720px) {
    .nav {
      left: 14px;
      right: 14px;

      padding: 0 18px;
    }

    .cta {
      display: none;
    }

    .wordmark {
      letter-spacing: 0.28em;
    }
  }
  .brand-lockup {
  display: flex;
  align-items: center;
  gap: 14px;
}

.brand-symbol {
  font-size: 15px;
  font-weight: 520;
  letter-spacing: -0.04em;

  color: rgba(245,247,250,0.94);
}

.brand-divider {
  width: 1px;
  height: 14px;

  background:
    linear-gradient(
      180deg,
      transparent,
      rgba(255,255,255,0.18),
      transparent
    );

  opacity: 0.5;
}

.brand-wordmark {
  font-size: 11px;

  letter-spacing: 0.52em;

  text-transform: uppercase;

  color: rgba(245,247,250,0.82);
}
</style>
<script>
  const nav = document.querySelector(".nav");

  if (nav) {
    window.addEventListener("scroll", () => {
      if (window.scrollY > 24) {
        nav.classList.add("scrolled");
      } else {
        nav.classList.remove("scrolled");
      }
    });
  }
</script>
